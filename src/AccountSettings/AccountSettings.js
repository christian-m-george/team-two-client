import React from 'react';

export default function AccountSettings() {
    return (
        <div>
            This is AccountSettings component
            <p>implement change email link that connects to middleware route (already set up)</p>
            <p>implement change password link that connects to middleware route (already set up)</p>
            <p>what other kind of things should be in the account settings component?</p>
        </div>
    )
}